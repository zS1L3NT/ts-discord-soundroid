# Issues to deal with

The bot is not updating the /play command appropriately. For messages it sends "SounDroid is thinking..." and for ephemeral commands it sends a non-ephemeral message "SounDroid is thinking...".